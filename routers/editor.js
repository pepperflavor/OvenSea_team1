const express = require("express");
const path = require("path");
const router = express.Router();

const editorController = require("../controllers/editor");

// editor 신청한 작가이름 받기
router.get("/editor-apply", );

// editor 신청하기 - 정보 넘겨주기
router.post("/editor-apply");

module.exports = router;